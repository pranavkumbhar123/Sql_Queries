# length of string
select length("hello") as length;

# concat string
select concat("Pranav"," ","Kumbhar") as fullName;

# indexing = in sql indexing starts from 1
select instr("java","a");

# substrings : start index, length
select substr("pra)